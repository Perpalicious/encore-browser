# Categorized JSON drop folder

After running the scraper, upload the raw JSON to your Auction Agent in ChatGPT
Enterprise. It will return an enriched JSON with categorization, Bat's List
flags, and Nice Picks. Save that file here as
`auction_{AUCTION_ID}_categorized.json`, then run the build script.

This folder is gitignored (except for this README and the provided sample).
